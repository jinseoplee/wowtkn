spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: none

cors:
  allowed-origins:
    - "https://www.wowtkn.com"
    - "https://wowtkn.com"